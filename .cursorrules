# 念及 (Nian Ji) - AI 个人记忆克隆项目宪法

## 技术栈
- **前端框架**: Next.js (App Router)
- **样式方案**: Tailwind CSS
- **后端服务**: Supabase (Auth / Database / Storage)

## UI 规范（适配老年人）
- **字号**: 基础字号 18px 及以上
- **对比度**: 高对比度配色，确保文字清晰可读
- **背景色**: 米色调背景 (#F5F5DC)
- **交互反馈**: 温暖、柔和的视觉反馈，避免刺眼的动画效果
- **按钮尺寸**: 大尺寸触摸目标，方便点击

## 数据流规范
- **录音存储**: 所有录音文件必须存入 Supabase Storage 的 `recordings` 桶
- **数据记录**: 录音文件链接及 AI 摘要存入 `memories` 表
- **文件命名**: 录音文件按 `{user_id}/{timestamp}.webm` 格式命名

## 数据库规范
- **扩展性**: 所有记忆条目必须包含 `tags` (JSONB) 字段
- **用户关联**: 所有数据必须关联 `user_id`，支持多用户隔离
- **时间戳**: 使用 `timestamp with time zone` 确保时区正确

## 代码规范
- **可读性**: 代码必须具备极高的可读性，变量和函数命名清晰
- **注释**: 关键逻辑必须添加中文注释说明
- **类型安全**: 使用 TypeScript，确保类型完整
- **错误处理**: 所有异步操作必须有完善的错误处理

## 安全规范
- **环境变量**: 敏感信息必须存放在 `.env.local` 中
- **RLS**: Supabase 表必须启用 Row Level Security
- **文件权限**: Storage 桶必须配置正确的访问策略
