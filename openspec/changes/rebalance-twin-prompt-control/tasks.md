## 1. Prompt 收敛

- [x] 1.1 重构 `/api/twin/chat` 的 system prompt builder，压缩显式控制条款，只保留少量硬边界
- [x] 1.2 把 progression、recency、fallback 等系统术语改写为自然语言提示，不再原样喂给模型
- [x] 1.3 为高风险语音输入保留更严的真相边界，但避免把普通低风险问题也一起写硬

## 2. Context Packing 调整

- [x] 2.1 把 twin chat 输入拆成“真相包”和“对话包”，只保留当前问题最相关的 facts、events、semantic evidence
- [x] 2.2 减少无关摘要和重复上下文的注入，避免模型把数据库摘要原样复述出来
- [x] 2.3 保留现有 debug 输出，但明确区分“给模型看的提示”和“给工程看的诊断字段”

## 3. Few-shot 与自然表达

- [x] 3.1 为“刚聊过再问”“同日重问”“几天后再问”“没有新角度时自然收束”增加少量 few-shot 示例
- [x] 3.2 验证 same topic 追问时，分身能补充或换角度，而不是机械重复
- [x] 3.3 验证普通非风险问题下，分身回答更像自然叙述，而不是日志式执行结果

## 4. 验证与文档

- [x] 4.1 增加对比用例，验证调整前后在同一话题短轮对话中的自然度差异
- [x] 4.2 更新 QA 指南，加入“像人程度”“是否仍守真相边界”“是否出现幻觉回潮”的验证步骤
- [x] 4.3 更新相关 OpenSpec 任务状态，并记录回滚策略与观察指标
