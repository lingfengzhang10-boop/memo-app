## 1. 人物列表与路由基建

- [x] 1.1 抽出统一的 `DialogueCharacter` 模型，并实现移动端可用的人物列表加载逻辑，仅覆盖 Liva 与已授权人物。
- [x] 1.2 为“已授权人物”补齐最小的数据来源与权限规则，使当前用户只能看到明确授权给自己的分身。
- [x] 1.3 为分身补充分身照片素材的存储与读取来源，确保授权人物卡片和分身详情页有独立形象可展示。
- [x] 1.4 新建并梳理 Figma 对齐的移动端路由，明确 `/mobile`、人物选择、更多、用户信息、分身详情和 bootstrap 页面之间的跳转关系。

## 2. 统一对话页

- [x] 2.1 将 `/mobile` 重构为 Figma 对齐的统一对话页，默认展示文字模式，并提供与语音模式的显式切换。
- [x] 2.2 在统一对话页里复用现有 Liva 与分身两条业务链路，确保人物切换后仍走各自正确的后端逻辑。
- [x] 2.3 在新对话页中保留顶部人物回复文本、底部用户文本回显、语音回放/错误态和线索确认态，不削弱现有确认边界。

## 3. 次级页面

- [x] 3.1 实现人物选择页，显示 Liva、当前用户自己的分身与已授权人物，显示当前激活人物的蓝色高亮，并在切换后返回对应人物的统一对话页。
- [x] 3.2 实现“更多”页的“未创建分身 / 已有分身”双态分支，并接入创建分身、分身详情和用户信息入口。
- [x] 3.3 将 `app/mobile/twin/[id]` 改为分身详情页，展示分身独立照片素材，提供“与分身对话”主操作，并把其余操作做成预留态。
- [x] 3.4 在分身创建流程中加入照片上传，确保创建完成后分身详情和授权人物页都能读取到该素材。

## 4. 预留态与样式收口

- [x] 4.1 按 Figma 调整移动端整体样式与布局，并从主入口移除旧底部导航的依赖。
- [x] 4.2 为附件加号、本地图片/视频入口、用户信息扩展位和其他未细化操作实现明确的预留态，而不是空白或死链。
- [ ] 4.3 完成移动端手工验证，覆盖文字/语音切换、Liva 对话、分身对话、无分身/有分身分支和人物切换回跳。
