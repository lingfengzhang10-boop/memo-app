## 1. 服务端修复

- [x] 1.1 更新 `/api/twin/voice/clone` 的 `customName` 生成逻辑，只输出上游允许的 ASCII 字母、数字、`_` 和 `-`
- [x] 1.2 为超长或纯本地化分身名增加长度裁剪和安全默认回退，避免音色上传因名字非法而失败

## 2. 验证

- [x] 2.1 验证分身音色克隆接口在现有前端调用下仍返回原有字段结构
- [x] 2.2 运行至少一项静态校验，确认修复未引入 TypeScript 或构建级错误
